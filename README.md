## WSCLI command line tool and SDK

`wscli-php-sdk` includes beefed up Swagger generated ISECure
WS-Channel API client SDK.

Command line tool `wscli` uses `wscli-php-sdk` and is distributed as
single file PHAR.
